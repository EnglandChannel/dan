$('.remove-product').bind('ajax:success', function() {
    $('#cart-icon').text("<%= cart_count_over_one %>");
    $('#cart-subtotal').text("<%= number_to_currency(total_price) %>");
    $(this).closest('.product').fadeOut();
});